class Prsn {
  // ptivate -> 클래스 내에서만 유효
  private name: string;
  public age: number;
  // readonly -> 읽기 가능, 수정 불가
  readonly log: string;

  constructor(name: string, age: number) {
    this.name;
    this.age;
  }
}
